# website-simpel

De website (PrinciplesAI) staat volledig in **`react-app/`** (Vite + React).

```bash
cd react-app
npm install
npm run dev
```

Productiebuild:

```bash
cd react-app
npm run build
```

Output staat in `react-app/dist/`.
